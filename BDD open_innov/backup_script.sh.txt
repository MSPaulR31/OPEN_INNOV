#!/bin/bash

# Informations de connexion à la BDD
DB_HOST="192.168.1.36"
DB_USER="backup"
DB_PASSWORD="debian"
DB_NAME="open_innov"

DATE=`date +%d-%m-%y_%H:%M:%S`
# On sauvegarde notre base de données
mysqldump --host=$DB_HOST --user=$DB_USER --password=$DB_PASSWORD $DB_NAME > /h>

# S'il y a plus de 4 sauvegardes, on supprime la plus ancienne
ls -t open_innov_*.sql | tail -n +5 | xargs rm f